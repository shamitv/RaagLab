# D05 plan: operations and handoff

Write tested start/stop/status/log/update/rollback instructions, model revision
update procedure, graceful worker drain, WSL restart behavior, logical database
backup and isolated restore, and the final portability matrix. Keep model weights,
database, artifacts, and configuration backup boundaries explicit and never delete
volumes during ordinary updates.
