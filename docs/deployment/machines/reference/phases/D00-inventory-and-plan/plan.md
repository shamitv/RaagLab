# D00 plan: inventory and plan

Inventory the target without exposing secrets, choose one container topology,
select a feasible model from primary documentation, define the deployment phases,
and record the exact exit gates. Preserve the portable application's mock evidence
as a separate deployment history.

Tasks: inspect WSL/Docker/GPU/storage; record observed facts and assumptions;
compare model requirements with hardware; establish paths and provider boundaries;
write D01–D05 plans and status files; link all evidence.

Exit gate: target, model decision process, dependency gaps, recovery implications,
and acceptance checks are explicit. No service or model state is changed by D00.
